  .tea-detail-row:not(.hidden) { display: table-row; animation: detailExpand 0.3s ease-out; }
  @keyframes detailExpand { from { opacity: 0; transform: translateY(-8px); } to { opacity: 1; transform: translateY(0); } }
  .expand-icon { transition: transform 0.25s ease; }
  .tea-row:focus-visible { outline: 2px solid var(--color-leaf-500); outline-offset: -2px; border-radius: 0; }
